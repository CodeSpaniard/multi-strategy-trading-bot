paper_mode: true
asset_class: crypto

symbols:
  - BTC/USD
  - ETH/USD

strategy:
  mode: trend
  ma_fast: 20
  ma_slow: 100
  take_profit_pct: 40.0
  stop_loss_pct: 15.0
  trailing_stop_pct: 12.0

risk:
  max_position_usd: 250
  daily_loss_limit_usd: 500
  max_trades_per_day: 999

loop:
  poll_seconds: 3600   # check once per hour (daily bars, no rush)
